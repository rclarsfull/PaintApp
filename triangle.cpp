#include "triangle.h"

Triangle::Triangle()
{

}

void Triangle::setPoint(QPoint point,int point)
{
    if(point < 0 || point > 4)
        return;
    if(point == 4)
        orign
}

QPoint Triangle::getPoint(int point)
{

}
